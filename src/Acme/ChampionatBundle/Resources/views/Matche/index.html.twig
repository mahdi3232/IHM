{% extends '::base.html.twig' %}

{% block titre -%}
Joueur list

{% endblock %}


{% block panelbody -%}
<div class="panel-body">
    <p class="bg-success">Ligue:<br>
        Saison:<br>
        Journeé:<br></p>
</div>
{% endblock %}




{% block tableau -%}
 <table class="table table-hover" >
<thead>
    <tr>
        <th>Equipe 1</th>
        <th>Date</th>
        <th>Heure</th>

        <th>Equipe 2</th>

    </tr>
</thead>
<tbody>
        {% for entity in entities %}
        <tr class="info" >
            <td width="40%">{{ entity.idEquipe }}</td>

            <td width="10%"><a href="{{ path('matche_show', { 'id': entity.id }) }}">{{ entity.date|date('Y-m-d') }}</a></td>
            <td width="10%"><a href="{{ path('matche_show', { 'id': entity.id }) }}">{{ entity.date|date('H:i') }}</a></td>


            <td width="40%">{{ entity.idEquipe2 }}</td>

        </tr>
        {% endfor %}
    </tbody>
</table>
    {% endblock %}
