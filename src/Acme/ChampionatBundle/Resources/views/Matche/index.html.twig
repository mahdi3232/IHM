{% extends '::base.html.twig' %}

{% block titre -%}
Joueur list

{% endblock %}


{% block panelbody -%}
<div class="panel-body">
    <p class="bg-success">Ligue: {{ entityLigue.nomLigue }}<br>
        Saison: <br>
        Journeé:<br></p>
</div>
{% endblock %}




{% block tableau -%}
<table class="table table-hover" >
    <thead>
        <tr>
            <th>Equipe 1</th>
            <th>Date</th>
            <th>Heure</th>

            <th>Equipe 2</th>

        </tr>
    </thead>
    <tbody>
    {% set i = 0 %}
        {% set j = 0 %}
        {% for entity in entities %}
          {% set i = i+1 %}
            <tr class="info" >
                <td width="40%">           
 {% for entityeq in entity4 %}
                  {% if entityeq.id is sameas(i) %}
                        <img src={{asset('bootstrap/fonts/logoEquipeMin/')}}{{ entityeq.id }}.png>

                  {% endif %}
                {% endfor%}
 {{ entity.idEquipe }}</td>

                    <td width="10%"><a href="{{ path('matche_show', { 'id': entity.id }) }}">{{ entity.date|date('Y-m-d') }}</a></td>
                    <td width="10%"><a href="{{ path('matche_show', { 'id': entity.id }) }}">{{ entity.date|date('H:i') }}</a></td>


                    <td width="40%">

 {% set j = i+1 %}
 {% for entityeq in entity4 %}
 {% if entityeq.id is sameas(j) %}
                        <img src={{asset('bootstrap/fonts/logoEquipeMin/')}}{{ entityeq.id }}.png>

                  {% endif %}
                {% endfor%}
{{ entity.idEquipe2 }}</td>

                </tr>
        {% endfor %}
            </tbody>
        </table>
    {% endblock %}
